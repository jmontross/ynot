# frozen_string_literal: true

source "https://rubygems.org"

ruby ">= 3.0"

gem "sinatra", "~> 4.0"      # web framework
gem "rackup", "~> 2.1"       # `rackup`/config.ru runner for Rack 3
gem "puma", "~> 6.4"         # app server (used in dev and on AWS)

group :development do
  gem "rerun", "~> 0.14"     # auto-reload during development
end
